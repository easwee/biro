export const formatterUSD = new Intl.NumberFormat("en-US", {
  style: "currency",
  currency: "USD"
});

export const formatterEUR = new Intl.NumberFormat("en-US", {
  style: "currency",
  currency: "EUR"
});
